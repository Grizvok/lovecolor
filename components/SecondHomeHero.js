import React from 'react';

import ContactInformation from './ContactInformation';

export default () => (
  <section className="hero is-small bg-img">
    <div className="hero-head herotextcontainer">
      <div className="container has-text-centered">
        <ContactInformation />
      </div>
    </div>
  </section>
);
